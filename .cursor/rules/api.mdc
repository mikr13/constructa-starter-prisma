---
description: 
globs: src/routes/api/*.ts,src/routes/api/**/*.ts
alwaysApply: false
---
- Every HTTP endpoint must be declared with createServerFileRoute from @tanstack/react-start/server and exported as ServerRoute. No other import or export names are permitted.

Required pattern:
```
import { json } from "@tanstack/react-start";
import { createServerFileRoute } from "@tanstack/react-start/server";

export const ServerRoute = createServerFileRoute("/api/widgets").methods({
  GET: async ({ request, params, context }) => {
    /* …business logic… */
    return json([{ id: 1, name: "Ada" }]);
  },
  POST: async ({ request }) => {
    const body = await request.json();
    /* …create widget… */
    return json({ ok: true }, { status: 201 });
  },
});
```

Forbidden patterns:
```
// ❌ Old helper
import { createAPIFileRoute } from "@tanstack/react-start/api";

// ❌ Wrong export name
export const APIRoute = createServerFileRoute("/api/widgets");
```